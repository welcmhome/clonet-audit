# Pre-audit form submissions are sent to you via Telegram.
# Optional: if not set, the form still "succeeds" but no message is sent.

# Create a bot: message @BotFather on Telegram, send /newbot, follow prompts.
# Paste the token here (e.g. 123456789:ABCdefGHI...).
TELEGRAM_BOT_TOKEN=

# Your chat ID: message your bot (e.g. /start), then open:
#   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# Find "chat":{"id": 123456789} and paste that number here.
# For a group, add the bot to the group and use the group's chat ID (usually negative).
TELEGRAM_CHAT_ID=
